# Tell Cucumber where to find feature files and step glue when using the JUnit Platform engine
# Use a glob to ensure all .feature files under the features folder are picked up on the classpath
cucumber.features=classpath:features/**/*.feature
cucumber.glue=steps
cucumber.plugin=pretty,json:target/cucumber-report.json
